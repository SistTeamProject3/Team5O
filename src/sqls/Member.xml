<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">

<!-- 	<select id="login" 
		parameterType="sist.co.model.MemberDTO"
		resultType="sist.co.model.MemberDTO">
		SELECT ID, NAME, EMAIL, AUTH FROM SISTMEMBER
		WHERE m_id=#{m_id} OR m_phone=#{m_phone} AND PWD=#{pwd}
	</select>
	
	<insert id="addMember" 
		parameterType="sist.co.model.MemberDTO">
		INSERT INTO TP_SISTMEMBER(ID, NAME, PWD, EMAIL, AUTH)
		VALUES(#{m_id}, #{m_name}, #{m_nickname}, #{m_password},#{m_phone}, SYSDATE,
		 #{m_birthday}, #{m_profile},#{m_highschool}, #{m_university}, #{m_office}, #{m_marriage},
		 #{m_address}, #{m_deactivation}, #{m_ask},#{m_content}, #{m_stopdate})	
	</insert> -->

	
	<select id="check_member" parameterType="sist.co.model.MemberDTO" resultType="java.lang.Integer">
		SELECT NVL(COUNT(*),0)
		FROM TP3_MEMBER WHERE M_ID=#{m_id}
	</select>

</mapper>  




